<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase / # Set your subdirectory here

    # Redirect to the public folder
    RewriteRule ^$ public/ [L]
    RewriteRule (.*) public/$1 [L]
</IfModule>
Options -Indexes
